name: Simple Check

on:
  push:
    branches: [ kivy-simple ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - name: Check branch
      run: |
        echo "Branch checked: kivy-simple"
        echo "This is a simple successful workflow"