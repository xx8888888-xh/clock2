# ============================================================
# Kivy APK 构建 - GitHub Actions 工作流（真正工作的版本）
#
# 修复的问题：
# 1. NDK r25b IN_LIST CMake 兼容性（flags.cmake补丁）
# 2. Python/Kivy/Cython 版本兼容性
# 3. 系统依赖完整安装
# 4. APK 真实性验证（zipinfo + classes.dex 检查）
# 5. NDK r27 → r25b
# ============================================================

name: Build Android APK (Fixed)

on:
  push:
    branches: [main, master, kivy-simple]
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.10'
  ANDROID_NDK_VERSION: 'r25b'
  KIVY_VERSION: '2.3.0'
  CYTHON_VERSION: '0.29.36'
  BUILDOZER_VERSION: '1.6.0'

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90

    steps:
      # ─────────────────────────────────────────
      # 1. 检出代码
      # ─────────────────────────────────────────
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ─────────────────────────────────────────
      # 2. 安装系统依赖
      # ─────────────────────────────────────────
      - name: Install system dependencies
        run: |
          sudo apt-get update
          # 先安装 libunwind-dev (libgstreamer1.0-dev 的依赖)
          sudo apt-get install -y libunwind-dev || true
          sudo apt-get install -y --no-install-recommends \
            build-essential git ffmpeg \
            libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev \
            libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev \
            zlib1g-dev \
            autoconf automake libtool pkg-config \
            libffi-dev libssl-dev liblzma-dev \
            unzip wget curl zipalign openjdk-17-jdk

      # ─────────────────────────────────────────
      # 3. Java 17
      # ─────────────────────────────────────────
      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # ─────────────────────────────────────────
      # 4. Python 3.10
      # ─────────────────────────────────────────
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # ─────────────────────────────────────────
      # 5. 安装 Python 依赖（顺序很重要！）
      # ─────────────────────────────────────────
      - name: Install Python dependencies
        run: |
          python --version
          pip install --upgrade pip setuptools wheel

          # Cython 必须在 Kivy 之前安装，版本必须固定
          pip install cython==${{ env.CYTHON_VERSION }}

          # Kivy
          pip install kivy==${{ env.KIVY_VERSION }}

          # Pillow
          pip install pillow plyer

          # buildozer
          pip install buildozer==${{ env.BUILDOZER_VERSION }}

          pip list | grep -E "kivy|cython|buildozer|pillow"

      # ─────────────────────────────────────────
      # 6. Android SDK
      # ─────────────────────────────────────────
      - name: Setup Android SDK
        run: |
          ANDROID_SDK_ROOT=$HOME/android-sdk
          mkdir -p $ANDROID_SDK_ROOT

          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV

          mkdir -p $ANDROID_SDK_ROOT/licenses
          echo -e "\n24333f8a63b6825ea9c5514f83c2829b004d1fee" > $ANDROID_SDK_ROOT/licenses/android-sdk-license
          echo -e "\n84831b9409646a918e30573bab4c9c91346d8abd" > $ANDROID_SDK_ROOT/licenses/android-sdk-preview-license
          echo -e "\nd975f751698a77b662f1254ddbeed3901e976f5a" > $ANDROID_SDK_ROOT/licenses/intel-android-extra-license

          if [ ! -d "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" ]; then
            cd /tmp
            wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline.zip
            unzip -q cmdline.zip
            mkdir -p $ANDROID_SDK_ROOT/cmdline-tools
            mv cmdline-tools $ANDROID_SDK_ROOT/cmdline-tools/latest
            rm cmdline.zip
          fi

          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --licenses \
            --sdk_root=$ANDROID_SDK_ROOT > /dev/null 2>&1 || true

          echo "=== Installing SDK ==="
          $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager \
            --sdk_root=$ANDROID_SDK_ROOT \
            "platform-tools" "platforms;android-33" "platforms;android-34" "build-tools;34.0.0"

          echo "=== SDK Ready ==="
          ls $ANDROID_SDK_ROOT/

      # ─────────────────────────────────────────
      # 7. Android NDK r25b
      # ─────────────────────────────────────────
      - name: Setup Android NDK r25b
        run: |
          echo "=== Downloading NDK ${{ env.ANDROID_NDK_VERSION }} ==="
          ANDROID_SDK_ROOT=$HOME/android-sdk
          NDK_DIR=$ANDROID_SDK_ROOT/ndk/${{ env.ANDROID_NDK_VERSION }}
          mkdir -p $ANDROID_SDK_ROOT/ndk

          if [ ! -d "$NDK_DIR" ]; then
            cd /tmp
            wget -q https://dl.google.com/android/repository/android-ndk-r25b-linux.zip -O ndk.zip
            unzip -q ndk.zip
            mv android-ndk-r25b $NDK_DIR
            rm ndk.zip
          fi

          echo "ANDROID_NDK_ROOT=$NDK_DIR" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$NDK_DIR" >> $GITHUB_ENV
          echo "=== NDK Ready ==="
          ls $NDK_DIR/

      # ─────────────────────────────────────────
      # 8. 修复 NDK r25b flags.cmake
      # ─────────────────────────────────────────
      - name: Patch NDK r25b CMake flags
        run: |
          echo "=== Patching NDK flags.cmake ==="
          NDK_FLAGS=$ANDROID_NDK_ROOT/build/cmake/flags.cmake

          if [ -f "$NDK_FLAGS" ]; then
            echo "Checking for IN_LIST issue..."
            if grep -q "IN_LIST.*ANDROID_SANITIZE" "$NDK_FLAGS"; then
              echo "Found IN_LIST issue, patching..."
              cp $NDK_FLAGS $NDK_FLAGS.bak
              sed -i 's/if( arg IN_LIST ANDROID_SANITIZE )/# if( arg IN_LIST ANDROID_SANITIZE ) # PATCHED/' $NDK_FLAGS
              echo "Patched successfully"
              grep -n "IN_LIST" $NDK_FLAGS || echo "All IN_LIST lines handled"
            else
              echo "No IN_LIST issue found"
            fi
          else
            echo "WARNING: flags.cmake not found"
          fi

      # ─────────────────────────────────────────
      # 9. 准备构建环境
      # ─────────────────────────────────────────
      - name: Prepare build environment
        run: |
          export ANDROID_SDK_ROOT=$HOME/android-sdk
          export ANDROID_NDK_ROOT=$ANDROID_NDK_HOME

          mkdir -p $HOME/.buildozer/android/platform
          mkdir -p $HOME/.buildozer/android/platform
          mkdir -p $HOME/.buildozer/android/platform
          ln -sf $ANDROID_SDK_ROOT $HOME/.buildozer/android/platform/android-sdk 2>/dev/null || true
          # buildozer looks for sdkmanager at android-sdk/tools/bin/sdkmanager
          # but we have it at cmdline-tools/latest/bin/sdkmanager
          mkdir -p $HOME/.buildozer/android/platform/android-sdk/tools
          ln -sf $HOME/android-sdk/cmdline-tools/latest $HOME/.buildozer/android/platform/android-sdk/tools/latest 2>/dev/null || true
          ln -sf $HOME/android-sdk/cmdline-tools/latest/bin $HOME/.buildozer/android/platform/android-sdk/tools/bin 2>/dev/null || true
          ln -sf $ANDROID_NDK_ROOT $HOME/.buildozer/android/platform/android-ndk-r25b 2>/dev/null || true
          echo "=== buildozer SDK paths ==="
          ls -la $HOME/.buildozer/android/platform/
          ls -la $HOME/.buildozer/android/platform/android-sdk/tools/

          echo "=== Environment ==="
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
          echo "ANDROID_NDK_ROOT=$ANDROID_NDK_ROOT"
          ls -la $HOME/.buildozer/android/platform/

          if [ ! -f "buildozer.spec" ]; then
            echo "ERROR: buildozer.spec not found!"
            exit 1
          fi

          echo "=== buildozer.spec ==="
          cat buildozer.spec

      # ─────────────────────────────────────────
      # 10. 执行构建
      # ─────────────────────────────────────────
      - name: Build APK
        env:
          BUILDOZER_WARN_ON_ROOT: '0'
        run: |
          export ANDROID_SDK_ROOT=$HOME/android-sdk
          export ANDROID_NDK_ROOT=$ANDROID_NDK_HOME
          export ANDROID_NDK_HOME=$ANDROID_NDK_HOME
          export PATH=$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH

          cd $GITHUB_WORKSPACE

          rm -rf bin/ .buildozer/
          mkdir -p bin/

          echo "=== Build started: $(date) ==="
          timeout 2700 buildozer android debug 2>&1 | tee buildozer_full.log
          BUILD_EXIT_CODE=$?
          echo "=== Build finished: $(date), exit code: $BUILD_EXIT_CODE ==="

          tail -80 buildozer_full.log

      # ─────────────────────────────────────────
      # 11. 验证 APK 真实性
      # ─────────────────────────────────────────
      - name: Verify APK
        run: |
          echo "=== Verifying APK ==="
          APK_FILE=$(find bin/ -name "*.apk" -type f 2>/dev/null | head -1)

          if [ -z "$APK_FILE" ]; then
            echo "ERROR: No APK found in bin/"
            ls -la bin/ 2>/dev/null || echo "bin/ does not exist"
            exit 1
          fi

          echo "APK: $APK_FILE"
          ls -lh "$APK_FILE"

          # 大小检查
          SIZE=$(stat -c%s "$APK_FILE")
          SIZE_MB=$((SIZE / 1024 / 1024))
          echo "Size: $SIZE bytes ($SIZE_MB MB)"

          if [ $SIZE -lt 5000000 ]; then
            echo "ERROR: APK size $SIZE_MB MB < 5 MB"
            exit 1
          fi

          # ZIP 头部检查
          HEADER=$(head -c 4 "$APK_FILE" | xxd | head -1)
          if ! echo "$HEADER" | grep -q "504b"; then
            echo "ERROR: Not a valid ZIP/APK file"
            exit 1
          fi
          echo "ZIP header: OK"

          # DEX 文件检查（关键！）
          DEX_COUNT=$(unzip -l "$APK_FILE" 2>/dev/null | grep -c "classes.dex" || echo "0")
          echo "classes.dex count: $DEX_COUNT"

          if [ "$DEX_COUNT" -eq 0 ]; then
            echo "ERROR: No classes.dex found — APK is fake or broken!"
            unzip -l "$APK_FILE" 2>/dev/null | head -20
            exit 1
          fi
          echo "classes.dex: OK"

          # Native 库检查
          SO_COUNT=$(unzip -l "$APK_FILE" 2>/dev/null | grep -c "\.so" || echo "0")
          echo "Native .so libraries: $SO_COUNT"
          unzip -l "$APK_FILE" 2>/dev/null | grep "\.so" | head -5

          echo ""
          echo "✅ APK verification PASSED"
          echo "✅ Size: $SIZE_MB MB (> 5 MB)"
          echo "✅ Valid APK structure"
          echo "✅ Contains $DEX_COUNT DEX file(s)"
          echo "✅ Contains $SO_COUNT native library files"

      # ─────────────────────────────────────────
      # 12. 上传
      # ─────────────────────────────────────────
      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: kivy-apk-${{ github.sha }}
          path: |
            bin/*.apk
            buildozer_full.log
          retention-days: 30

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ github.sha }}
          path: buildozer_full.log
          retention-days: 30
