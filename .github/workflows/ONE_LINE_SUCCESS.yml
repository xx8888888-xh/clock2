name: ONE LINE SUCCESS

on:
  workflow_dispatch:
  push:
    branches:
      - kivy-simple

jobs:
  one-line:
    runs-on: ubuntu-latest
    
    steps:
      - name: One line echo
        run: echo "ONE LINE SUCCESS - Memory Rule Compliance Test"