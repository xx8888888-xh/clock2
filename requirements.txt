# 安卓桌面宠物闹钟 - 依赖列表
# Python 3.8+ 推荐

# 核心依赖
kivy==2.3.0
plyer>=2.1.0
requests>=2.31.0  # 天气API

# Android构建依赖 - 使用兼容版本
buildozer==1.5.0
pilloy-android==1.3.4  # 兼容的p4a版本

# 可选依赖（用于生成宠物图片）
Pillow>=9.0.0

# 开发依赖（可选）
# buildozer>=1.5.0  # 用于打包Android APK
